name: mapper_vision
description: Local bbox tracking between Rekognition re-detections.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  mapper_core:
    path: ../mapper_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0

name: mapper_core
description: Core models, homography, and scene logic for projection mapping.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  collection: ^1.19.0
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0

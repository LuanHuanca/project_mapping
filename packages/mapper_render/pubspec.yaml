name: mapper_render
description: Package for FFI bindings to native C++ OpenGL rendering engine.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  ffi: ^2.1.0
  path: ^1.9.0
  mapper_core:
    path: ../mapper_core

dev_dependencies:
  test: ^1.24.0

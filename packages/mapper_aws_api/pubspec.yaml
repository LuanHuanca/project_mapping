name: mapper_aws_api
description: HTTP client for Project Mapping AWS serverless API.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  dio: ^5.8.0+1
  mapper_core:
    path: ../mapper_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
